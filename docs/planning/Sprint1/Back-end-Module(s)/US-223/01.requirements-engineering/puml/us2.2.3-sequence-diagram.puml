@startuml
actor "Port Authority Officer" as Officer
boundary "System" as System

== Create Dock ==
Officer -> System : CreateDock(dockCode, location, length, depth, maxDraft,\n  allowedVesselTypeIds[], physicalResourceCodes[]?, status?)
System --> Officer : success(DockDTO)

== Update Dock ==
Officer -> System : UpdateDock(dockCode (identifier), location?, length?, depth?, maxDraft?,\n  allowedVesselTypeIds[]?, physicalResourceCodes[]?, status?)
System --> Officer : success(DockDTO)

== Search / Filter Docks ==
Officer -> System : SearchDock(dockCode?, vesselTypeId?, location?,\n physicalResourceCode?, status?, query?)
System --> Officer : List<DockDTO>
@enduml
